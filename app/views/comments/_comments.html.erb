<!--Can use comment instead of @comments because the object @comments is being passed-->
<!--in when the partial is called in recipe show page-->

<% comments.each do |comment| %>
  <div class="row">
    <div class="col-sm-2">
      <%= gravatar_for(comment.chef, size: 40) %>
    </div>
    <div class="col-sm-10">
      <p><%= comment.body %></p>
      <span class="quiet"><small>
        [posted <%= time_ago_in_words(comment.created_at) %> ago
        by <%= comment.chef.chefname.capitalize %>]
      </small></span>
    </div>
  </div>
  <hr>
<% end %>

<%= will_paginate(comments, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer) %>
